
version: 2

sources:
  - name: raw_gsheets
    loader: google_sheets
    tables:
      - name: transactions_raw

  - name: spotify
    tables:
      - name: fct_play
        columns:
        - name: "play_id"
          meta:
            metrics:
              total_order_count:
                type: count_distinct

